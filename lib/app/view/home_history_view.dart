import 'package:flutter/material.dart';

class HomeHistoryView extends StatelessWidget {
  const HomeHistoryView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('HomeHistoryView'),);
  }
}